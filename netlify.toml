[build]
  publish = ".next"
  command = "npm run build:netlify"

[build.environment]
  NODE_VERSION = "22"

# Netlify manages HTTPS and the primary-domain redirect. Duplicating those
# redirects here can create a loop when the site's configured primary domain
# changes between the apex and www hostnames.
